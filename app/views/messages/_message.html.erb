<div id="message-<%= message.id %>" class="<%= message.user == current_user ? "hey" : "mey" %>" >
  <small>
    <strong><%= message.user.nickname %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
